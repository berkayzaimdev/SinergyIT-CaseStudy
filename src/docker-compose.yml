services:
  sonarqube:
    image: sonarqube

  sonarqubedb:
    image: postgres:13

  catalog.api:
    image: ${DOCKER_REGISTRY-}catalogapi
    build:
      context: .
      dockerfile: Services/Catalog/Catalog.API/Dockerfile

networks:
  sonarnetwork: